export type GoldTempRow = {
  id: string;
  code: string;
  type: string;
  price: number;
  unit: string;
  side: string;
  releaseDate: string;
  location: string;
  description: string;
  quantity: number;
};